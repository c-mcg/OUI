#include "TestUtil.h"

#include "event/OUI_Event.h"

using namespace oui;

TEST(EventDispatcher, it_can_be_created)
{
    // Event event("test", 2);
    // EXPECT_TRUE(event.compareType("test"));
    // EXPECT_TRUE(event.compareType(2));
}
